export const theme = {
    colors: {
        primary: "#c0c0c0",
        black: "#000000",
    },
    button: {
        light: "#e0e0e0",
        normal: "#808080",
        dark: "#000000",
    }
};

// Infer the type of the theme
export type ThemeType = typeof theme;
